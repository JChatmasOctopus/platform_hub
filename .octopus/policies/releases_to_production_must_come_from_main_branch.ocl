name = "Releases to production must come from main branch"
violation_action = "block"

conditions {
    rego = <<-EOT
            package releases_to_production_must_come_from_main_branch
            
            # Default: Deny all deployments
            default result := {"allowed": false}
            
            # Allow: If the release is associated with a specific Git reference
            result := {"allowed": true} if {
            	# Target a specific branch, tag, or commit hash for the release
            	input.Release.GitRef == "<Release-GitRef>"
            }
            EOT
}

scope {
    rego = <<-EOT
            package releases_to_production_must_come_from_main_branch
            
            default evaluate := true
            
            # The following are examples of available scoping options:
            # evaluate if {    
            	# Scope evaluation by Environment name    
            	# input.Environment.Name == "<environment-name>"    
            	# Scope evaluation to Space Id    
            	# input.Space.Id == "<space-id>"
            	# Scope evaluation to multiple Spaces
                # input.Project.Slug in ["<project-slug>", "<project-slug2>"]
            # }
            EOT
}